# Frozen snapshot

These files are the token layer as delivered in M5.5b, kept so the `.dc.html`
references in `docs/design-system/` still load. They are not kept in step
with the app. The token values the app ships are in `src/styles/tokens/`, and
§1.12 of `docs/spec/ui-i18n.md` names those.
