# Journal — 2026-07-25 — Plan split and repository bootstrap

## What happened

- Initialized the git repository on `main` and committed `plan.md` as the
  baseline (`529cdaa`), then split it into the loop-agent document set and
  archived the original at `docs/archive/complete-plan.md`.
- The split was mechanical: a script sliced `plan.md` by heading line-ranges,
  so content is verbatim except for the deliberate deviations below. A
  heading-coverage check confirmed every §-heading survived the split.

## Deliberate deviations from the original plan text

- §2.1 canonical file map: replaced the stale `docs/superpowers/plans/…`
  subtree with a one-line pointer to the actual `docs/` layout, and added
  `AGENTS.md` to the root file list.
- §4.4 (branch/commit/PR conventions) lives only in `AGENTS.md`;
  `docs/plan/github.md` points to it rather than duplicating it.
- §7.5 (wave monitoring) lives in `docs/plan/roadmap.md`, not `AGENTS.md`,
  because it gates waves rather than single-task iterations.
- §10 (execution handoff) was folded into `docs/plan/roadmap.md`.
- Container headings §1, §2, §4, §6, §7, §8 were dropped; all of their content
  is preserved under the original numbered subsections.
- Every task file gained a `reads:` YAML frontmatter listing the spec files
  (with section hints) that its implementation agent must load.

## Decisions (Ori)

- git init approved; bootstrap commits made directly on `main` with explicit
  approval (no remote or branch protection exists yet).
- Full GitHub workflow chosen: labels, milestones, and one issue per task per
  `docs/plan/github.md`.
- Execution mode: subagent-driven development — fresh implementation agent per
  task, specification review + quality review at every task boundary
  (loop-engineering recommendation, accepted 2026-07-25).

## Open items

- GitHub remote does not exist yet; repository name and visibility are Ori's
  call. After creation: labels, milestones, and T01–T14 issues per
  `docs/plan/github.md`.
- Vercel CLI is not installed; required at T14 (`npm i -g vercel`).
- `/usr/bin/python3` is 3.9 — `X | None` annotation syntax fails; use
  `from __future__ import annotations` in local tooling scripts.
