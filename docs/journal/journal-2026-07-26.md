# Journal — 2026-07-26 — Production release

## Release record

- Production: <https://ozterisk.vercel.app> — deployment of exactly `eb6cc67`,
  status READY, Vercel project `cgm-16s-projects/ozterisk`.
- All 14 tasks merged via reviewed PRs; issues #1–#14 closed; milestones
  M0–M3 closed. CI (`build`) green on the release commit and now a required
  status check on protected `main`.
- Final whole-branch review (37e8329..eb6cc67): READY TO DEPLOY — 0 Critical,
  0 Important; all 26 deferred minors triaged ACCEPT (11 already resolved).
- §8.6 production smoke: PASSED in real Chrome — ko auto-detection (§1.13),
  full loop (correct → overflow → discard → incorrect → game over with
  terminal equation retained and excluded from rounds), Copy Result inline
  status, refresh → title with manual-only language persistence (exactly one
  storage key), zero non-origin network requests across a full run. Native
  Share sheet not machine-clicked (OS modal freezes automation); its logic is
  unit-tested — Ori can verify by hand.

## Deploy quirks worth remembering

- A brand-new Vercel project's FIRST `vercel deploy` (no `--prod`) landed
  directly in the Production environment. Deployment-specific URLs sit behind
  Vercel Authentication (302); the canonical domain is public.
- `vercel link` silently appends `.vercel` and `.env*` to `.gitignore` —
  committed via chore PR after the fact.
- `gh` needed `workflow` scope to push `.github/workflows/` (Ori ran
  `gh auth refresh -s workflow`).

## Open follow-ups (post-release, none blocking)

- Spec amendment question for Ori: §1.11 gameOver Enter restarts even when
  Share/Copy is focused (keyboard-UX trap; ships as specified, test-locked).
- Final-review minor tidy batch: START_RUN phase-guard comment; always-mounted
  status container for answering→feedback announce; terminal equation outside
  landmark; fixture dedup into src/test/fixtures.ts; drop unused --radius-lg.
- No monitoring/drains configured — intentional (spec excludes analytics).
