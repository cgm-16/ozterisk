# 2026-09-06 — M5.5d, Board Surfaces

## State

Branch `feat/M5.5d-board-surfaces`, off `29708b8`. All four implementation tasks
landed; **`T42` has not run.** It was dispatched and died partway through Step 1
on a session rate limit, having committed nothing — the working tree was clean
afterwards, so there is no partial state to untangle. Its own task file is
current and its brief still stands; it needs re-dispatching, not rewriting.

| Commit | What |
|---|---|
| `8107f76` | T38 — ten fixed sockets, `liftedIds`, the `GameScreen` derivation |
| `51b4277` | the tier boundary, moved out of T38 (below) |
| `91ae5f9` | T39 — answer slot rim, equation display type |
| `efdae49` | T40 — `CapacityMeter`, HUD gold streak, `hud.capacity` copy |
| `56504d5` | two corrections to T40 (below) |
| `e692b5a` | T41 — segmented control ported, focus ring cleared on gold |
| `ddf1974` | T42's task file re-pointed at the moved boundary |

`npm run lint && npm run typecheck && npm test && npm run build` green at
`ddf1974`; 261 tests across 20 files. Zero diff under `src/game/`.

## The one spec amendment

**§1.12 stated its own rule correctly and then contradicted it with its worked
example.** The rule: a tier's rack width — five tiles plus four gaps — must
leave room for the arena's horizontal padding at that tier's lower bound. The
example: *"`378px` inside `400px`"*. But the arena spends `24px` on padding
below `40rem`, so `400px` leaves `376px`, and `378 > 376`.

Two pixels of horizontal overflow, in the `400`–`401px` window.

Nothing caught it before because the rack was `flex-wrap`: five tiles that did
not fit wrapped to a third row. Be precise about what that means — it was not
this same overflow hidden from view, it was a **different** behaviour. The rack
silently degraded to a layout §1.12 does not describe, and no scrollbar ever
appeared. T38 made the grid fixed at five columns, which is the whole point of
the task, and a fixed grid has nowhere to wrap to, so the shortfall surfaces as
overflow.

So: the spec's arithmetic was wrong from the day it was written, and T38 changed
which symptom it produces — a silent relayout became a visible two pixels. That
distinction is the argument for amending the spec rather than the code.

The tile and gap values are the design's own and stand. The boundary was the
wrong number, so the boundary moved: **`408px`**, above the `402px` the
arithmetic bottoms out at so a desktop window narrow enough to show a vertical
scrollbar still clears, and below `412px` so that phone tier keeps the larger
tile.

Amended in `docs/spec/ui-i18n.md` §1.12 and `src/styles/tokens/spacing.css`.
**This is a spec change made mid-milestone on the orchestrator's judgment**, on
the grounds that it is an arithmetic correction rather than a design decision —
the same class of false claim `T27` corrected twice in this handover. Flagged to
Ori as reversible.

## Two corrections to T40, both a token reached for where it does not fit

**The pip radius.** `--radius-sm` is `5px`; the pip is `7px` tall. At that
height the token stops being a radius: CSS scales radii down when two on one
side exceed the side, so `5px` clamps to `3.5px` — exactly half the height — and
the design's softened rectangle renders as a pill. Now a literal `2px`, beside
the width and height already written that way. A dedicated token would be YAGNI:
one fixed shape in one place, and unlike the tile it does not move across tiers.

**The HUD kept the UI sans.** `typography.css` names its own territory —
*"Numerals — equations, tile faces, HUD values. Always the display serif"* —
and HUD values were the last of the three still unconverted. `T35` did tile
faces, `T39` did the equation. Family only; sizes untouched, so `dd.primary`'s
specificity tie against `.entry dd` still resolves to the computed values
`App.test.tsx:139` reads.

## Measurements taken so far

Not a substitute for `T42`, which re-takes all of these independently.

| Claim | Figure | Floor | By |
|---|---|---|---|
| Slot rim `--border-slot-empty` over `--surface-socket` | `4.80:1` | `3:1` | T39, from pixels + recomputed from hex |
| Equation at `320px`, widest case, en and ko | `195px` of `296px` | must fit | T39, rendered |
| Focus ring, gold on inactive segment | `11.85:1` | `3:1` | T41 |
| Focus ring, `--clay-900` on gold active segment | `5.68:1` | `3:1` | T41 |
| Inactive segment label, small text | `5.18:1` | `4.5:1` | T41 |

**What passed on the gold segment is worth stating precisely.** The gold band is
invisible there at `1.51:1`, so the entire visible indicator is the `1px` dark
line the composition leaves at `3px` inset. It clears SC 1.4.11, which is the
gate. It is still `1px`. `T42` is asked to report the band's thickness rather
than only its ratio, so the record carries what passed and not merely that it
did.

## Carried forward

- **`T42` owes seven measurements**, including `407`/`408` specifically — an
  off-by-one at the moved boundary restores exactly the defect just fixed.
- Badge contrast: gold-on-ceramic still owes `4.5:1`. Carried since `M5.5c`,
  deliberately. → `M5.5e`.
- The rack holds **eleven** mid-overflow and lands the eleventh alone on a third
  row. The design's answer is the perched tile on `OverflowControls`. → `M5.5e`.
- **`--size-label` is being used outside its stated territory.** `typography.css`
  documents it as *"mono, uppercase, tracked"*; `T41` applies it with
  `--font-ui` to non-uppercase mixed-script labels (`English` / `한국어`). It
  measures `5.18:1` and clears its small-text floor, so nothing is broken — but
  either the token's comment no longer describes its use or the toggle wants a
  different size. Settle it in `M5.5g` rather than rediscovering it there.
- Issue #77 (stylelint) stays blocked on `M5.5g`.

## For whoever re-dispatches T42

Do not re-run it as one seven-step sweep; that is what burned ~90 turns and died
in Step 1. The steps are not equally expensive. Steps 1, 2, 3, 5 and 6 are
geometry and computed style — a page resize, `getBoundingClientRect` per cell, a
`getComputedStyle` read, two document properties — and they are also the three
claims the exit gate actually names. Step 4 is the screenshot-diff pipeline that
ate `T37`'s hours, and it is the one step `T41` has already measured from pixels
using the method the journal specifies. Split them: one agent for 1/2/3/5/6,
a second for 4 alone. If budget is tight, **Step 4 is the one to defer** — its
remaining value is independent-read discipline, not a missing number.

Check for orphaned dev servers before starting one. Two were live when this was
written.

## T42, part A — Steps 1, 2, 3, 5, 6 (Step 4 is a second agent's, still outstanding)

Ports `5173`/`5174` were the orphans; `npm run dev` auto-incremented to `5175` and
that server was stopped before this entry was written. All readings via Playwright
against `/gallery.html`, which composes the real `GameScreen`/`GameHud`/
`TileInventory`/`CapacityMeter` components from fixture-built `GameState`s — no
token values read, no computed style inferred from a screenshot. Zero diff under
`src/`.

### Step 1 — ten sockets at three tiers, and the boundary

`TileInventory.tsx`'s `cellCount = Math.max(INVENTORY_CAPACITY, tiles.length)`
means "one tile held" and "ten tiles held" aren't reachable from any existing
gallery entry at the low end — the closest is 8 tiles (`answering-full`). Rather
than infer the boundary is safe, a second `GameScreen` was mounted at runtime
alongside the gallery's own, fed a hand-built one-tile `GameState` via
`dynamic import()` of the same unmodified `/src/test/fixtures.ts` and
`/src/components/GameScreen/GameScreen.tsx` modules the app already loads (no
file touched, no new fixture added to `states.tsx`). Both racks were read at
every width; the synthetic mount was unmounted before Step 2.

| Viewport | `--tile-w` | Rack width | Cells (1 held) | Cells (10 held) | `scrollWidth` vs `clientWidth` |
|---|---|---|---|---|---|
| `320px` | `52px` | `292px` | `10` | `10` | `305 / 305` — equal |
| `407px` | `52px` | `292px` | `10` | `10` | `392 / 392` — equal |
| `408px` | `66px` | `378px` | `10` | `10` | `393 / 393` — equal |
| `768px` | `64px` | `368px` | `10` | `10` | `753 / 753` — equal |

Ten cells every time, at both ends of the held range. The `407`/`408` pair is
the one the brief flagged: `407` still renders the narrow tile (`52px`, rack
`292px`); `408` is the first width at the wide tile (`66px`, rack `378px`), one
pixel of viewport apart. No off-by-one — `378px` fits inside the space `408px`
leaves. (`clientWidth` runs 15px under the nominal viewport at every row above
because a real vertical scrollbar is present — the page is taller than `900`–
`1000px` at these widths, which is expected and unrelated to horizontal fit;
`scrollWidth === clientWidth` at every row is the actual claim, and it holds.)

### Step 2 — a selected tile keeps its socket

Read via `getBoundingClientRect()` on all ten rack cells, at `768px`, across
the gallery's own `answering-empty` → `answering-partial` → `answering-full` →
`answering-partial` (**return**) → `answering-empty` (**clear**) sequence —
each a real `GameState` produced by running `gameReducer`'s `SELECT_TILE` /
`RETURN_TILE`, not a hand-built fixture. `TileInventory` is fed
`sortTiles([...inventory, ...selectedTiles])` with `liftedIds` marking the
selected ones, so a selected tile's cell keeps its position in the sorted
array and only swaps face-for-socket in place — that's the mechanism this step
checks.

**40 of 40 cell comparisons across the four transitions were pixel-identical**
(`x`, `y`, `width`, `height` all equal, not merely close): 10 cells ×
(select tile-1, select tile-2, return tile-2, clear tile-1). The lifted cell
itself also held its rect across the transition where it changed state (e.g.
cell index 1, `Digit 1` → `lifted-socket`, same `[428.5, 400, 64, 80]` before
and after). The full round trip landed back on byte-identical rects to the
opening baseline.

### Step 3 — round retains primary emphasis

Read via `getComputedStyle` on the three `<dd>` elements in `GameHud`, no
inference from the rule text:

| Field | `dd` class | Computed `font-size` |
|---|---|---|
| Round | `.primary` | **`24px`** |
| Score | (none) | `20px` |
| Streak | (none) | `20px` |

`dd.primary` (round) wins the `(0,1,1)` tie against `.entry dd` by source
order, as `GameHud.module.css`'s comment says it should. Round renders at
`1.2×` score/streak. This is exactly the tie the milestone flagged as one rule
away from silently flipping — confirmed resolved correctly today, not
inferred from the CSS.

### Step 5 — no horizontal scroll at 320px

`documentElement.scrollWidth` vs `clientWidth`, plus a sweep of every element
under the game `<main>` for `getBoundingClientRect().right > clientWidth`, at
`320px`, across `answering` (the `all slots filled` entry — the widest
answering case), `feedback` (`correct, with reward badges`), and `overflow`
(`required 1`), in both `en` and `ko` (toggled live via the gallery's own
`LanguageToggle`, not a reload with a forced `localStorage` key):

| Phase | Locale | `scrollWidth` | `clientWidth` | Overflowing elements |
|---|---|---|---|---|
| answering | en | `305` | `305` | `0` |
| answering | ko | `305` | `305` | `0` |
| feedback | en | `305` | `305` | `0` |
| feedback | ko | `305` | `305` | `0` |
| overflow | en | `305` | `305` | `0` |
| overflow | ko | `305` | `305` | `0` |

Six of six clean. The equation — the brief's named risk, since `--size-equation`
is `68px` and unclamped — measured `195.0625px` in both locales (glyphs are
ASCII digits/`×`/`=` regardless of language, so locale doesn't move this
number). That reproduces T39's `195px of 296px` figure independently. Worth
stating precisely: **`--size-equation` is not stepped down at the narrow
tier** — `grep` across every `.css` file found no media query touching it, and
the computed `font-size` at `320px` reads the full `68px`. The brief's "T39
may have stepped it down" did not happen; nothing needed it to, since `195px`
already clears the tier with room (`296`–`305px` available depending on
scrollbar), but it means there is no narrow-tier safety margin built in if the
equation ever needs another digit — a design headroom note, not a defect.

### Step 6 — eleven held

Gallery entry `overflow-required-1` (`INVENTORY_CAPACITY` 10 + `REWARD_BONUS`
1 = 11 tiles, Endless's actual mid-overflow shape):

- Rack (`TileInventory`) rendered **`11`** children, not `10`.
- Capacity text read **`"Capacity 11 / 10"`**.
- The meter's `role="img"` element carried `aria-label="Capacity 11 of 10"`
  and **`11`** pip children.

Both the rack and the meter distinguish eleven from ten; neither silently
clamped to the resting-state count.

### What this leaves for the second agent

Step 4 (the language toggle's ring on gold, per-tone contrast against both
segments) is **not measured here** — it belongs to a separate agent per the
dispatch split, and is still outstanding as of this entry.

### Method note

Step 1's one-tile-held case has no gallery fixture at that inventory size;
rather than skip the low end of the range or add a fixture (`src/gallery/
states.tsx` is under `src/`, off limits for this task), a second `GameScreen`
was mounted at runtime against a hand-built state, built entirely from modules
the app already ships. This produces a real DOM tree from the real component,
not a synthetic approximation — but it's worth flagging as unusual so a future
reader doesn't mistake it for a gallery entry that exists in the codebase.
